import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    primary: {
      main: "#1976d2", // Professional Blue
    },
    secondary: {
      main: "#ed6c02", // "Biscuit" Orange/Gold
    },
    background: {
      default: "#f4f6f8",
    },
  },
});

export default theme;
